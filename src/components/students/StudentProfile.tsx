import { CalendarCheck2, CheckCircle2, CreditCard, Mail, MapPin, Phone, QrCode, Radio, ShieldCheck, UserRound, X } from 'lucide-react'
import type { Student } from '../../types/student'
import { StatusBadge } from './StatusBadge'

interface StudentProfileProps { student?: Student; onClose: () => void; onEdit: (student: Student) => void; onStatusChange: (student: Student, status: Student['status']) => void }

const timelineIcon = { registration: UserRound, attendance: CalendarCheck2, status: ShieldCheck, rfid: CreditCard }
const timelineColor = { registration: 'bg-blue-50 text-blue-600 dark:bg-blue-500/10 dark:text-blue-300', attendance: 'bg-emerald-50 text-emerald-600 dark:bg-emerald-500/10 dark:text-emerald-300', status: 'bg-amber-50 text-amber-600 dark:bg-amber-500/10 dark:text-amber-300', rfid: 'bg-violet-50 text-violet-600 dark:bg-violet-500/10 dark:text-violet-300' }

export function StudentProfile({ student, onClose, onEdit, onStatusChange }: StudentProfileProps) {
  if (!student) return null
  const name = `${student.firstName} ${student.middleName ? `${student.middleName[0]}. ` : ''}${student.lastName}`
  return <div className="fixed inset-0 z-40 bg-slate-950/40 backdrop-blur-[1px]" onMouseDown={onClose}><aside onMouseDown={(event) => event.stopPropagation()} role="dialog" aria-modal="true" aria-labelledby="profile-title" className="ml-auto flex h-full w-full max-w-xl flex-col overflow-y-auto bg-white shadow-2xl dark:bg-slate-900"><header className="sticky top-0 z-10 flex items-center justify-between border-b border-slate-200 bg-white/95 px-5 py-4 backdrop-blur dark:border-slate-800 dark:bg-slate-900/95"><div><p className="text-[10px] font-bold uppercase tracking-[.14em] text-blue-600">Student profile</p><h2 id="profile-title" className="mt-0.5 text-base font-bold text-slate-900 dark:text-white">Student information</h2></div><button onClick={onClose} className="rounded-lg p-2 text-slate-400 hover:bg-slate-100 dark:hover:bg-slate-800" aria-label="Close student profile"><X size={19} /></button></header><div className="space-y-6 p-5"><section className="flex gap-4"><div className={`grid size-16 shrink-0 place-items-center overflow-hidden rounded-2xl text-lg font-bold ${student.avatarColor}`}>{student.photo ? <img src={student.photo} alt={`${name} profile`} className="size-full object-cover" /> : `${student.firstName[0]}${student.lastName[0]}`}</div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h3 className="text-lg font-bold text-slate-900 dark:text-white">{name}</h3><StatusBadge status={student.status} /></div><p className="mt-1 font-mono text-xs text-slate-400">{student.studentId}</p><p className="mt-1 text-xs text-slate-500">{student.courseCode} · {student.yearLevel} · {student.section}</p></div></section><div className="grid grid-cols-2 gap-2"><button onClick={() => onEdit(student)} className="rounded-xl bg-blue-600 py-2.5 text-xs font-bold text-white shadow-lg shadow-blue-600/20 hover:bg-blue-700">Edit information</button><select value={student.status} onChange={(event) => onStatusChange(student, event.target.value as Student['status'])} className="rounded-xl border border-slate-200 bg-white px-3 text-xs font-bold text-slate-600 outline-none focus:border-blue-400 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-200"><option>Active</option><option>Inactive</option><option>Suspended</option><option>Archived</option></select></div><section className="rounded-2xl bg-linear-to-br from-blue-600 to-indigo-600 p-4 text-white"><div className="flex items-start justify-between"><div><p className="text-xs font-semibold text-blue-100">Attendance percentage</p><p className="mt-2 text-3xl font-bold">{student.attendance.attendanceRate}%</p><p className="mt-1 text-[11px] text-blue-100">Current semester performance</p></div><div className="grid size-14 place-items-center rounded-2xl bg-white/15"><CalendarCheck2 size={25} /></div></div><div className="mt-4 grid grid-cols-4 gap-1 border-t border-white/15 pt-3 text-center text-[10px]"><span><b className="block text-sm">{student.attendance.present}</b>Present</span><span><b className="block text-sm">{student.attendance.late}</b>Late</span><span><b className="block text-sm">{student.attendance.absent}</b>Absent</span><span><b className="block text-sm">{student.attendance.excused}</b>Excused</span></div></section><section><h3 className="text-sm font-bold text-slate-800 dark:text-slate-100">Contact information</h3><div className="mt-3 grid gap-2"><p className="flex items-center gap-2 rounded-xl bg-slate-50 px-3 py-2.5 text-xs text-slate-600 dark:bg-slate-950/50 dark:text-slate-300"><Mail size={15} className="text-blue-500" />{student.email}</p><p className="flex items-center gap-2 rounded-xl bg-slate-50 px-3 py-2.5 text-xs text-slate-600 dark:bg-slate-950/50 dark:text-slate-300"><Phone size={15} className="text-blue-500" />{student.contactNumber}</p><p className="flex items-start gap-2 rounded-xl bg-slate-50 px-3 py-2.5 text-xs text-slate-600 dark:bg-slate-950/50 dark:text-slate-300"><MapPin size={15} className="mt-0.5 shrink-0 text-blue-500" />{student.address}</p></div></section><section><h3 className="text-sm font-bold text-slate-800 dark:text-slate-100">Attendance credentials</h3><div className="mt-3 grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-slate-200 p-3 dark:border-slate-700"><div className="flex items-center gap-2 text-xs font-bold text-slate-700 dark:text-slate-200"><QrCode size={17} className="text-blue-600" />QR code</div><div className="mt-3 grid aspect-square max-w-28 place-items-center rounded-lg bg-slate-950 p-2"><div className="grid size-full grid-cols-5 gap-0.5 rounded-sm bg-white p-1">{Array.from({ length: 25 }, (_, index) => <i key={index} className={`${(index * 7 + student.id.length) % 3 ? 'bg-slate-950' : 'bg-white'}`} />)}</div></div><p className="mt-2 font-mono text-[10px] text-slate-400">{student.qrCode}</p></div><div className="rounded-xl border border-slate-200 p-3 dark:border-slate-700"><div className="flex items-center gap-2 text-xs font-bold text-slate-700 dark:text-slate-200"><Radio size={17} className="text-violet-600" />RFID card</div><p className="mt-4 font-mono text-xs font-semibold text-slate-600 dark:text-slate-300">{student.rfidNumber}</p><p className="mt-2 text-[10px] text-emerald-600 dark:text-emerald-400"><CheckCircle2 size={12} className="mr-1 inline" />Linked and active</p></div></div></section><section><h3 className="text-sm font-bold text-slate-800 dark:text-slate-100">Recent attendance</h3><div className="mt-3 overflow-hidden rounded-xl border border-slate-200 dark:border-slate-700">{student.attendance.recent.map((record) => <div key={`${record.date}-${record.time}`} className="flex items-center justify-between gap-2 border-b border-slate-100 px-3 py-3 last:border-0 dark:border-slate-800"><div><p className="text-xs font-semibold text-slate-700 dark:text-slate-200">{record.date} · {record.time}</p><p className="mt-0.5 text-[10px] text-slate-400">{record.method} · {record.device}</p></div><span className={`rounded-full px-2 py-1 text-[10px] font-bold ${record.status === 'Present' ? 'bg-emerald-50 text-emerald-700 dark:bg-emerald-500/10 dark:text-emerald-300' : record.status === 'Late' ? 'bg-amber-50 text-amber-700 dark:bg-amber-500/10 dark:text-amber-300' : 'bg-blue-50 text-blue-700 dark:bg-blue-500/10 dark:text-blue-300'}`}>{record.status}</span></div>)}</div></section><section><h3 className="text-sm font-bold text-slate-800 dark:text-slate-100">Student timeline</h3><ol className="mt-4 space-y-4 border-l border-slate-200 pl-4 dark:border-slate-700">{student.timeline.map((event) => { const Icon = timelineIcon[event.type]; return <li key={event.id} className="relative"><span className={`absolute -left-6.25 grid size-5 place-items-center rounded-full ring-4 ring-white dark:ring-slate-900 ${timelineColor[event.type]}`}><Icon size={11} /></span><p className="text-xs font-bold text-slate-700 dark:text-slate-200">{event.title}</p><p className="mt-1 text-[11px] leading-4 text-slate-400">{event.description}</p><time className="mt-1 block text-[10px] font-medium text-slate-400">{event.date}</time></li> })}</ol></section></div></aside></div>
}
