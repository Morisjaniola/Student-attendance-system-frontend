import { Cell, Pie, PieChart, ResponsiveContainer, Tooltip } from 'recharts'
import type { DashboardData } from '../../types/dashboard'
import { formatNumber } from '../../utils/format'

export function PieChartCard({ data }: { data: DashboardData['distribution'] }) {
  const total = data.reduce((sum, item) => sum + item.value, 0)
  return <section className="rounded-2xl border border-slate-200/80 bg-white p-5 shadow-sm dark:border-slate-800 dark:bg-slate-900"><div><h2 className="text-base font-bold text-slate-800 dark:text-slate-100">Attendance distribution</h2><p className="mt-1 text-xs text-slate-400">Today&apos;s recorded attendance</p></div><div className="relative mt-2 h-[185px]"><ResponsiveContainer width="100%" height="100%"><PieChart><Pie data={data} dataKey="value" nameKey="name" cx="50%" cy="50%" innerRadius={52} outerRadius={72} paddingAngle={4} stroke="none">{data.map((entry) => <Cell key={entry.name} fill={entry.color} />)}</Pie><Tooltip contentStyle={{ borderRadius: 10, border: '1px solid #e2e8f0', fontSize: 12 }} /></PieChart></ResponsiveContainer><div className="pointer-events-none absolute inset-0 grid place-items-center text-center"><span className="text-xl font-bold tracking-tight text-slate-900 dark:text-white">{formatNumber(total)}<small className="block text-[10px] font-medium uppercase tracking-wide text-slate-400">students</small></span></div></div><div className="grid grid-cols-2 gap-x-3 gap-y-2 border-t border-slate-100 pt-4 dark:border-slate-800">{data.map((item) => <div key={item.name} className="flex items-center justify-between text-xs"><span className="flex items-center gap-1.5 text-slate-500"><i className="size-2 rounded-full" style={{ backgroundColor: item.color }} />{item.name}</span><span className="font-bold text-slate-700 dark:text-slate-200">{Math.round((item.value / total) * 100)}%</span></div>)}</div></section>
}
