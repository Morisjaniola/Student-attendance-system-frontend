/**
 * Shared form styling — the single source of truth for field, label, and
 * error classes used across every form, filter, and modal in the app.
 * Compose with layout utilities (e.g. `${fieldClass} mt-1.5`).
 */
export const fieldClass =
  'h-10 w-full rounded-xl border border-slate-200 bg-white px-3 text-xs text-slate-700 outline-none transition placeholder:text-slate-400 focus:border-blue-400 focus:ring-4 focus:ring-blue-600/10 dark:border-slate-700 dark:bg-slate-950 dark:text-slate-100'

export const labelClass = 'block text-[11px] font-bold text-slate-600 dark:text-slate-300'

export const fieldErrorClass = 'mt-1 text-[10px] font-medium text-rose-600'
